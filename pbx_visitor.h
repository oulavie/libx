#ifndef PBX_VISITOR_H
#define PBX_VISITOR_H

// template< typename T > void visit( T& t_ ) { t_.visit( *this ); }
// template< typename T, typename... TArgs> void visit( T& t_, TArgs... args ) { t_.visit( *this, std::forward<TArgs>(args)...); }

#endif

// vim: set expandtab tabstop=2 shiftwidth=2 autoindent smartindent:

